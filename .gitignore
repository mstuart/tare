/target

# fastembed downloaded ONNX models (large, regenerated on demand)
.fastembed_cache/

# local tooling artifacts (vexp index + config)
/.vexp/
vexp.toml

# internal build scaffolding (AI session plans / specs / ledger) — not for public consumption
/docs/superpowers/

# Python bytecode
__pycache__/
*.pyc

# npm wrapper: downloaded binaries + deps (regenerated on install)
/npm/vendor/
/npm/node_modules/
/npm/*.tgz
