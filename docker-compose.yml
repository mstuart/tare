services:
  cull-proxy:
    build: .
    image: cull-proxy:latest
    ports:
      - "8787:8787"
    environment:
      CULL_UPSTREAM: https://api.anthropic.com
      CULL_PORT: "8787"
      # CULL_CONTEXT_LIMIT: "200000"   # set lower for smaller-window models
    restart: unless-stopped
