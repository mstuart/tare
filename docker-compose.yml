services:
  tare-proxy:
    build: .
    image: tare-proxy:latest
    ports:
      - "8787:8787"
    environment:
      TARE_UPSTREAM: https://api.anthropic.com
      TARE_PORT: "8787"
      # TARE_CONTEXT_LIMIT: "200000"   # set lower for smaller-window models
    restart: unless-stopped
